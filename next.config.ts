// Import the NextConfig type from Next.js for type-safe configuration
import type { NextConfig } from "next";

// Define the Next.js configuration object
// This object contains all the configuration options for the Next.js application
const nextConfig: NextConfig = {
  // Configuration options can be added here to customize Next.js behavior
  // Examples include:
  // - experimental: Enable experimental features
  // - images: Configure image optimization settings
  // - env: Define environment variables available in the browser
  // - redirects: Set up URL redirects
  // - rewrites: Configure URL rewrites
  // - webpack: Customize webpack configuration
  // Currently using default settings with no custom configuration
};

// Export the configuration object as the default export
// Next.js will automatically use this configuration when building and running the app
export default nextConfig;
